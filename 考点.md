指针 this
双指针

指针作为成员
    new, delete
    遍历 delete
    delete[]

是否需要析构

基类无脑加 virtual ，确保派生类的析构函数会被调用

std的容器 vector （map, list, set这次不考）

iterator 重点 - 灵活
    借力法：派生类通过复用基类（或底层对象）已经实现的方法，来完成自己的功能，而不是重新实现同样的逻辑。
    重载++等等