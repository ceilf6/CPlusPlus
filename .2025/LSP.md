Liskov Substitution Principle 里氏替换原则

如果 S 是 T 的子类型，那么任何使用 T 的地方，都可以用 S 替换
即：凡是能用 基类对象的地方，都必须能无感知地使用派生类

在语法中经常体现为 virtual

1. 派生类不能削弱基类的行为契约
    virtual ... = 0; 抽象类中的纯虚函数必须覆盖
        有一个成员函数是纯虚的那么这个基类就不能实例化，就是抽象类

例如 print 等函数子类中也一般会进行覆盖从而个性化输出

2. 基类的析构函数必须是 virtual 虚函数
从而确保调用子类的析构函数